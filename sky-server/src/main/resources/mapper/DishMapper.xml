<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.DishMapper">
    <!-- 分页查询菜品 -->
    <select id="page" resultType="Dish">
        SELECT * FROM dish
        <where>
            <if test="name != null and name != ''">
                name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>

    <!-- 动态更新菜品（非null字段才会更新） -->
    <update id="update">
        UPDATE dish
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="createUser != null">
                create_user = #{createUser},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 新增菜品，useGeneratedKeys使用数据库自增主键，keyProperty将主键注入到Dish对象的id字段 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish (name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
        VALUES (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <!-- 根据id查询菜品（含分类名称和口味列表），LEFT JOIN关联分类和口味表 -->
    <select id="getById" resultMap="dishGetByIdVOMap">
        SELECT
            d.id, d.name, d.category_id, d.price, d.image, d.description, d.status, d.update_time,
            c.name AS category_name,
            df.id AS df_id,
            d.id AS df_dish_id,
            df.name AS df_name,
            df.value AS df_value
        FROM dish d
        LEFT JOIN category c ON d.category_id = c.id
        LEFT JOIN dish_flavor df ON d.id = df.dish_id
        WHERE d.id = #{id}
    </select>

    <!-- 菜品详情结果映射，<collection>处理一对多关联（一个菜品→多个口味） -->
    <resultMap id="dishGetByIdVOMap" type="com.sky.vo.DishGetByIdVO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="price" property="price"/>
        <result column="image" property="image"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="update_time" property="updateTime"/>
        <collection property="flavors" ofType="com.sky.entity.DishFlavor">
            <id column="df_id" property="id"/>
            <result column="df_dish_id" property="dishId"/>
            <result column="df_name" property="name"/>
            <result column="df_value" property="value"/>
        </collection>
    </resultMap>

    <!-- 根据id集合批量删除菜品 -->
    <delete id="deleteByIds">
        DELETE FROM dish WHERE id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
